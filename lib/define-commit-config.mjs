// @ts-check

/**
 * @typedef {import('../types/define-configs.d.ts').CommitConfig} CommitConfig
 */

/**
 * * Define commit config for NHB Commit tool.
 * @param {CommitConfig} config Configuration object
 * @returns {CommitConfig}
 */
export function defineCommitConfig(config) {
	return config;
}
